# Daily — 27/04/2026

## Ontem (26/04)

- Fundação Sprint 0 completa: Next.js 14 + TS + Tailwind + CI/CD
- Banco de dados completo: 4 migrations, 6 tabelas, RLS em todas, 2 buckets
- Backend completo: validators Zod, camada server, 13 API routes, auth middleware
- UI completa seguindo design handoff: 15+ páginas/componentes
- 65+ casos de teste passando
- PR #3 (`feature/upload-fotos-google-ai`) criado e mergeado na main
- QA revisou e aplicou melhorias direto na máquina → PR #4 mergeado

## Hoje — o que foi feito

### Revisão QA (PR #4 — `fix/base64-photo-upload`) ✅

Analisei as 38 alterações do QA e identifiquei as categorias:

**Correções críticas:**
- Upload de fotos: substituído flow 3 etapas (sign → PUT → confirm) por base64 via POST unificado — eliminava CORS/401
- `@google/generative-ai` SDK removido do `google-ai.ts` (migrado para native fetch REST API — sem SDK pesado)
- `lib/env.ts`: bug de typo em `TURNSTILE_SECRET_KEY`, adicionado `_resetEnvCache()` para testes
- `validators/loja.ts`: z.preprocess para WhatsApp (remove espaços/parênteses), Instagram/TikTok (strip `@`)
- `validators/peca.ts`: `fotoBase64UploadSchema` + `precoStringToCentavos` corrigido
- `modal.tsx`: layout shift de scrollbar corrigido (`paddingRight` compensado + `scrollbar-gutter: stable`)
- `input.tsx`: `Omit<..., 'prefix'>` para evitar conflito com prop nativa HTML

**Melhorias de funcionalidade:**
- Vitrine pública: cards são agora `<Link>` clicáveis + foto real via signed URL
- Galeria de fotos: guard `if (!active) return null` evitando render desnecessário
- TryOnModal: retry em erro, suporte a `garmentImageUrlOverride`, fixes responsivos mobile
- `listOwnPecas`: retorna `foto_principal_url` (signed URL) para o admin
- `PublicLiveRefresh`: novo componente — auto-refresh RSC a cada 30s + ao focar a aba

**Infraestrutura:**
- Supabase helpers: removido `<Database>` generic redundante (server, middleware, service)
- `tsconfig.json` + `vitest.config.ts`: suporte a shim `server-only` nos testes
- `tests/shims/server-only.ts`: novo arquivo de shim para testes unitários
- API try-on: campo `garment_url_override` no schema

### Sincronização git com origin/main ✅

- HEAD estava quebrado (trailing spaces no arquivo .git/HEAD) — corrigido via Python
- Todos os 37 arquivos do QA sincronizados com `git show origin/main:path > path`
- `@google/generative-ai` removido do `package.json` (dep sem uso — QA migrou para native fetch)
- Commit `2815326` criado: `chore: remove unused @google/generative-ai dep`
- Script `push-main.ps1` criado na raiz do projeto para Francisco executar no Windows

### Estado final do git

```
2815326  chore: remove unused @google/generative-ai dep (replaced by native fetch)
8dfe246  Merge pull request #4 from francisco-efjr/fix/base64-photo-upload
f949dad  fix base64
d22a2ed  Merge pull request #3 from francisco-efjr/feature/upload-fotos-google-ai
a8810be  feat: upload de fotos + provador Google AI (Gemini) + galeria pública
```

## Bloqueado / esperando

- Contas externas (Supabase, Vercel, FASHN, Replicate, Upstash, Cloudflare, Sentry) — cliente
- **Push final**: rodar `push-main.ps1` no PowerShell do Windows para empurrar commit `2815326`
- Telas que o designer ainda não fez: recuperar senha, definir senha pós-convite, detalhe completo de peça
- Decisão sobre domínio definitivo
- Pasta `~/Documents/Vitrine Virtual` no host (código ainda em `_app/` dentro do vault)

## Bugs / dívidas técnicas conhecidas

- `listOwnPecas` faz 1 signed URL por peça no admin — N+1 aceitável para MVP, futuramente virar RPC
- Turnstile usa `'dev-bypass'` no front — precisa do site key real (cliente cria conta Cloudflare)
- Cron de kill switch automático (Vercel Cron) ainda não configurado
- Botão de logout usa form HTML — poderia virar Server Action
- Upload de logo da loja: bucket existe, UI faltando

## Próximo bloco

- [ ] Francisco roda `push-main.ps1` para subir commit para origin/main
- [ ] Documentação técnica em `docs/` (ARCHITECTURE, DATA-MODEL, SECURITY, ENV, PHOTO-GUIDELINES)
- [ ] README.md do projeto
- [ ] Smoke test e2e Playwright completo
- [ ] Quando cliente entregar contas: configurar Supabase prod + Vercel + variáveis de ambiente

## Métricas do vault

- Commits na main: 5 (incluindo 2 PRs mergeados)
- Arquivos no `_app/`: ~80 (código) + testes + config
- PR #3: upload de fotos + Google AI (Gemini)
- PR #4: 38 arquivos alterados pelo QA (base64 upload, validators, UI fixes)
- Dep removida: `@google/generative-ai` (^0.21.0) → native fetch

---
**Tags:** #projeto/vitrine-virtual #daily #2026-04-27
