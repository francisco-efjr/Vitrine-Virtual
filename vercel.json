{
  "buildCommand": "cd _app && pnpm install --frozen-lockfile && pnpm build",
  "outputDirectory": "_app/.next",
  "installCommand": "cd _app && pnpm install --frozen-lockfile",
  "framework": "nextjs"
}
